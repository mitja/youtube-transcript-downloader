{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -q '\\.env' && echo '{\"decision\":\"block\",\"reason\":\".env files are sensitive and should not be read.\"}' || true"
          }
        ]
      }
    ]
  }
}
